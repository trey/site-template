/*
Use this file to add enhanced CSS support to IE (or any browser).

An example:

	$('#nav li:first a').css('border', 'none');

Then in your normal stylesheet, you could just do:

	#nav li:first a { border: none; }

Include it in your HTML like so:

	<!--[if lte IE 6]><script src="/css/ie/css.js" type="text/javascript"></script><![endif]-->
*/

$(function() {
    // Stuff to do as soon as the DOM is ready;
});
